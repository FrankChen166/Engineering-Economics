<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= user.username %>的資料</title>
  </head>
  <body>
    <div>
      <table>
        <tr>
          <th>班級 : <%= user.CLASS %></th>
        </tr>
        <tr>
          <th>姓名 : <%= user.username %></th>
        </tr>
        <tr>
          <th>學號 : <%= user.studentId %></th>
        </tr>
        <% if (user.userInfo && user.userInfo.length > 0 &&
        user.userInfo[0].tests.length > 0) { %> <%
        user.userInfo[0].tests.forEach((test, index) => { %>
        <tr>
          <th>
            第 <%= index + 1 %> 次作業成績 : <%= test.testGrade %><a
              href="/info/<%= index%>"
            >
              Review</a
            >
          </th>
        </tr>

        <% }); %> <% } else { %>
        <tr>
          <th colspan="2">暫無作業成績</th>
        </tr>
        <% } %>
      </table>
      <a href="/home">回到主頁</a>
    </div>
  </body>
</html>
