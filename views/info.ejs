<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <title><%= user.username %>的資料</title>
  </head>
  <body>
    <div class="d-flex justify-content-center mt-5 text-center"">
      <ul class="list-group">
        <li class="list-group-item active " aria-current="true">
          學生資料
        </li>
        <li class="list-group-item ">班級 : <%= user.CLASS %></li>
        <li class="list-group-item ">姓名 : <%= user.username %></li>
        <li class="list-group-item ">學號 : <%= user.studentId %></li>
        <% if (user.userInfo && user.userInfo.length > 0 &&
        user.userInfo[0].tests.length > 0) { %> <%
        user.userInfo[0].tests.forEach((test, index) => { %>
        <li class="list-group-item">
          第 <%= index + 1 %> 次作業成績 : <%= test.testGrade %>
          <a href="/info/<%= index%>" style="text-decoration: none">
            Review</a
          >
        </li>
        <% }); %> <% } else { %>
        <li class="list-group-item">暫無作業成績</li>
        <% } %>
        <a href="/home" class="btn btn-primary btn-sm" style="color: black"
          >回到主頁</a
        >
      </ul>
    </div>
  </body>
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"
  ></script>
</html>
