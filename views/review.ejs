<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- head 部分 -->
    <style>
      .correct {
        color: green;
      }
      .incorrect {
        color: red;
      }
    </style>
  </head>
  <body>
    <!-- 页面内容 -->

    <div>
      <h2>Test <%= testTimes %></h2>
      <% requestedTest.answers.forEach((answer, index) => { %>
      <p><%= answer %></p>

      <% }); %>
    </div>
  </body>
</html>
