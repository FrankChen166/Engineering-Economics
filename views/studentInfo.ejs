<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <title><%= users.username %>的資訊</title>
  </head>
  <body>
    <div class="mt-3 text-center">
      <h3><%= users.username %>的資訊</h3>
    </div>
    <table class="table table-hover text-center mt-3 custom-margin">
      <tr>
        <th>班級 : <%= users.CLASS %></th>
      </tr>
      <tr>
        <th>姓名 : <%= users.username %></th>
      </tr>
      <tr>
        <th>學號 : <%= users.studentId %></th>
      </tr>

      <% if (users.userInfo && users.userInfo.length > 0 &&
      users.userInfo[0].tests.length > 0) { %> <%
      users.userInfo[0].tests.forEach((test, index) => { %>
      <tr>
        <th>第 <%= index + 1 %> 次作業成績 : <%= test.testGrade %></th>
      </tr>

      <% }); %> <% } else { %>
      <tr>
        <th colspan="2">暫無作業成績</th>
      </tr>
      <% } %>
    </table>
    <div class="text-center">
      <a href="/class/<%= users.CLASS %>" class="btn btn-primary btn-sm"
        >回上一頁</a
      >
    </div>
  </body>
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"
  ></script>
</html>
