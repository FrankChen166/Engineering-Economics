<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>學生名單</title>
  </head>
  <body>
    <h1>學生名單</h1>
    <% users.forEach((user) => { if (user.role !== "teacher") { %>
    <ul>
      <a href="/allStudent/<%= user._id %>"><li><%= user.username %></li></a>
    </ul>
    <% } }); %>
  </body>
</html>
